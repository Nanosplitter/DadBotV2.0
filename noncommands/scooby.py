class Scooby:
    async def whatsTheMove(self, bot):
        c = bot.get_channel(856919399789625376)
        await c.send("***What's The Move?***")
    
    async def praiseFireGator(self, bot):
        c = bot.get_channel(856919399789625376)
        await c.send("***PRAISE***")